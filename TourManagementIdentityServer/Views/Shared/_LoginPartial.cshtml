@using Microsoft.AspNetCore.Identity

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@model string

<!-- @if (User.Identity.IsAuthenticated)
{
    <form asp-area="qucikstart" asp-controller="Account" asp-action="Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post" id="logoutForm" class="navbar-right">
        <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
                <a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
            </li>
            <li class="dropdown">
                <button type="submit" class="btn btn-link navbar-btn navbar-link">Logout</button>
            </li>
        </ul>
    </form>
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li class="dropdown"><a asp-area="quickstart" asp-controller="account" asp-action="register">Register</a></li>
        <li class="dropdown"><a asp-area="quickstart" asp-controller="account" asp-action="login">Login</a></li>
    </ul>
} -->

@if (string.IsNullOrWhiteSpace(Model))
{
    <ul class="nav navbar-nav navbar-right">
        <li class="dropdown"><a asp-area="quickstart" asp-controller="account" asp-action="register">Register</a></li>
        <li class="dropdown"><a asp-area="quickstart" asp-controller="account" asp-action="login">Login</a></li>
    </ul>
}